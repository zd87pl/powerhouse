version: "3.8"
services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: powerhouse-chroma
    ports:
      - "8001:8000"
    volumes:
      - /data/powerhouse/chroma-data:/chroma/chroma
    environment:
      - CHROMA_SERVER_HOST=0.0.0.0
      - CHROMA_SERVER_PORT=8000
      - CHROMA_SERVER_CORS_ALLOW_ORIGINS=["*"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v2/heartbeat"]
      interval: 30s
      timeout: 10s
      retries: 3

  n8n:
    image: docker.n8n.io/n8nio/n8n:latest
    container_name: powerhouse-n8n
    ports:
      - "5678:5678"
    volumes:
      - /data/powerhouse/n8n-data/.n8n:/home/node/.n8n
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=${N8N_PASSWORD:-changeme}
      - N8N_HOST=${N8N_HOST:-localhost}
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://${N8N_HOST:-localhost}:5678/
    restart: unless-stopped
    depends_on:
      chromadb:
        condition: service_healthy

  # observability-bridge:
  #   build: ./services/observability-bridge
  #   container_name: powerhouse-bridge
  #   ports:
  #     - "8002:8000"
  #   environment:
  #     - SENTRY_AUTH_TOKEN=${SENTRY_AUTH_TOKEN:-}
  #     - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
  #     - GITHUB_TOKEN=${GITHUB_TOKEN:-}
  #   volumes:
  #     - /data/powerhouse/observability-bridge:/app/data
  #   restart: unless-stopped
  #   depends_on:
  #     chromadb:
  #       condition: service_healthy
